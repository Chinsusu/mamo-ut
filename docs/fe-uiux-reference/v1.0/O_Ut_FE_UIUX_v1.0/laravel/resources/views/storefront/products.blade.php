<x-layouts.storefront :title="$seoTitle" :description="$seoDescription" page="products">
    <section class="page-hero"><div class="container"><div class="breadcrumbs"><a href="{{ route('home') }}">Trang chủ</a><span>/</span><span>Sản phẩm</span></div><span class="eyebrow">Cửa hàng O Út</span><h1>Đặc sản Huế nhà làm</h1><p>Chọn theo món, xem giá và biến thể ngay trên danh sách.</p></div></section>
    <section class="section"><div class="container"><form class="shop-toolbar" method="GET"><div class="filter-chips">@foreach($categories as $category)<button class="filter-chip {{ request('category') === $category->slug ? 'is-active' : '' }}" name="category" value="{{ $category->slug }}">{{ $category->name }}</button>@endforeach</div><select class="sort-select" name="sort" onchange="this.form.submit()"><option value="featured">Nổi bật</option><option value="price-asc">Giá thấp đến cao</option><option value="price-desc">Giá cao đến thấp</option></select></form><div class="product-grid product-grid--4">@forelse($products as $product)<x-product-card :product="$product" />@empty<div class="empty-state"><h2>Chưa có sản phẩm phù hợp</h2></div>@endforelse</div>{{ $products->links() }}</div></section>
</x-layouts.storefront>
