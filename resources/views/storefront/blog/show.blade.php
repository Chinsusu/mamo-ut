@php($imageUrl = filled($post->image_path) ? Illuminate\Support\Facades\Storage::disk((string) config('commerce.media.public_disk'))->url($post->image_path) : asset('storefront-assets/images/blog-1.jpg'))
<x-storefront.layout :title="($post->seo_title ?? $post->title).' - '.config('app.name')" page="blog"><section class="page-hero page-hero--crumb"><div class="container"><div class="breadcrumbs"><a href="{{ route('home') }}">Trang chủ</a><span>/</span><a href="{{ route('blog.index') }}">Cẩm nang</a><span>/</span><span>{{ $post->title }}</span></div></div></section><section class="section"><div class="container article-page"><article class="article-body"><span class="eyebrow">Cẩm nang O Út</span><h1>{{ $post->title }}</h1><p class="muted">{{ $post->published_at?->format('d.m.Y') }}</p><img class="article-body__cover" src="{{ $imageUrl }}" alt="{{ $post->title }}">@if ($post->excerpt)<p>{{ $post->excerpt }}</p>@endif<div>{!! nl2br(e($post->body)) !!}</div></article><aside class="article-toc sidebar-card"><h2>Khám phá thêm</h2><p class="small muted">Xem các món được làm theo mẻ nhỏ, thông tin rõ ràng.</p><a class="btn btn--secondary btn--block" href="{{ route('products.index') }}">Xem sản phẩm</a></aside></div></section></x-storefront.layout>